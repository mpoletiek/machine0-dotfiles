# Program aliases
$terminal = kitty
$fileManager = thunar
$mainMod = SUPER

# Browser — change --profile-directory to "Profile 1" / "Profile 2" / etc. to pick a chromium profile.
# Profile dirs live in ~/.config/chromium-beta/
$browser = chromium-beta --profile-directory=Default
